# Approve native build scripts for vetted upstream packages.
# sharp        — Next.js image optimization (well-known, audited).
# unrs-resolver — ESLint's bundled module resolver.
auto-install-peers=true
